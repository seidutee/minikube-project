apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fashion-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/seidutee/minikube-project.git
    targetRevision: main
    path: apps/fashion-store-app
  destination:
    server: https://kubernetes.default.svc
    namespace: fashionapp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
