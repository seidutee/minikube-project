apiVersion: apps/v1
kind: Deployment
metadata:
  name: ayd-webapp-deployment
  labels:
    app: webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp
        image: seidut/ayd:1.0
        ports:
        - containerPort: 80
        